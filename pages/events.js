import Common from '../components/Common'

const Events = (props) => {
    return (
        <div>
            <Common />
            <p>Events List!</p>
        </div>
    )
}

Events.getInitialProps()

export default Events